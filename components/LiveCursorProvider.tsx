"use client";

function LiveCursorProvider({ children }: { children: React.ReactNode }) {
  return (
    <div>
      {/* render all cursors  */}

      {children}
    </div>
  );
}
export default LiveCursorProvider;
